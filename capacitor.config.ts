import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Labtest2_Emmanuel_Okonkwo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
